events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # Map for WebSocket Upgrade header
    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      close;
    }

    server {
        listen 80;
        listen 443 ssl;
        server_name localhost;

        ssl_certificate /etc/nginx/certs/selfsigned.crt;
        ssl_certificate_key /etc/nginx/certs/selfsigned.key;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;

        # Serve static web client files
        location / {
            root /usr/share/nginx/html;
            index index.html;
            try_files $uri $uri/ =404;
        }

        # Public endpoint to request OAuth 2.0 tokens from IDP
        location /oauth2/token {
            proxy_pass http://idp:5002/oauth2/token;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Internal-only sub-request handler for auth_request.
        # Forwards the Authorization header (REST) and the original query string
        # (which carries ?token=... for WebSocket requests) to the IDP.
        location = /oauth2/validate {
            internal;
            proxy_pass http://idp:5002/oauth2/validate;
            proxy_pass_request_body off;
            proxy_set_header Content-Length "";

            # IMPORTANT: proxy_set_header directives in this internal location block
            # are evaluated in the context of the PARENT (outer) request.
            # So $arg_token here correctly reads the ?token= from the original WS URL.
            # For REST calls, $arg_token is empty and Authorization header is used instead.
            proxy_set_header X-Token $arg_token;
            proxy_set_header Authorization $http_authorization;
            proxy_set_header X-Original-URI $request_uri;
        }

        # Proxy REST API requests to web-backend (Protected by OAuth 2.0)
        location /api/ {
            auth_request /oauth2/validate;

            # Extract user metadata headers from the IDP response
            auth_request_set $user_id $upstream_http_x_user_id;
            auth_request_set $user_role $upstream_http_x_user_role;

            # Inject these headers into the backend request
            proxy_set_header X-User-Id $user_id;
            proxy_set_header X-User-Role $user_role;

            proxy_pass http://web-backend:5000/api/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Proxy Centrifugo WebSocket connection (Protected by OAuth 2.0).
        # The browser passes the JWT as ?token=<JWT> in the WebSocket URL.
        # auth_request fires a sub-request to /oauth2/validate inheriting the
        # same query string (?token=...) which the IDP reads directly.
        location /connection/websocket {
            auth_request /oauth2/validate;
            auth_request_set $auth_status $upstream_status;

            proxy_pass http://centrifugo:8000;

            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # 1 hour timeout for WebSocket connections
            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
        }
    }
}
